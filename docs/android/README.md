# Android companion

Flutter app with:

- No `INTERNET` permission
- Storage / media permissions only when needed
- Allowlisted ops matching `never-format-core::companion`

PC remains the primary UI.
